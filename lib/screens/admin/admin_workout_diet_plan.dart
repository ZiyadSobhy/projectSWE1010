import 'package:flutter/material.dart';

class AdminWorkoutDietPlan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Workout & Diet Plans'),
      ),
      body: Center(
        child: Text('Admin workout and diet plans go here!'),
      ),
    );
  }
}
