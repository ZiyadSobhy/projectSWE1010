import 'package:flutter/material.dart';

class GoalSetting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Set Your Goals')),
      body: Center(
        child: Text('Set your fitness goals.'),
      ),
    );
  }
}
